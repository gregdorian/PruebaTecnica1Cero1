@model IEnumerable<WebMVCPrueba.Models.MunicipioTramite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Municipio.CodigoMunicipio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Municipio.NombreMunicipio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tramite.CodigoTramite)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tramite.NombreTramite)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Municipio.CodigoMunicipio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Municipio.NombreMunicipio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tramite.CodigoTramite)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tramite.NombreTramite)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
            @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.Id })
        </td>
    </tr>
}

</table>
